# 알림 시스템 종합 마이그레이션 요약 보고서

## 생성된 마이그레이션 파일

**파일명**: `supabase/migrations/notification/20260130000015_complete_notification_system_final.sql`

---

## 마이그레이션 파일에 포함된 내용

### 1. notification_type enum 생성
- **타입**: `TASK_CREATED`, `TASK_STATUS_CHANGED`, `TASK_DELETED`, `TASK_DUE_DATE_EXCEEDED`, `TASK_DUE_DATE_APPROACHING`
- **용도**: 알림 타입을 구분하는 enum

### 2. notifications 테이블 생성
- **컬럼**: id, user_id, notification_type, task_id, title, message, is_read, read_at, metadata, created_at
- **외래키**: user_id → profiles(id), task_id → tasks(id)
- **용도**: 모든 알림 데이터 저장

### 3. 인덱스 생성 (6개)
- `idx_notifications_user_id`: 사용자별 조회 최적화
- `idx_notifications_user_id_is_read`: 읽지 않은 알림 조회 최적화
- `idx_notifications_created_at`: 시간순 정렬 최적화
- `idx_notifications_task_id`: Task별 알림 조회 최적화
- `idx_notifications_notification_type`: 타입별 조회 최적화
- `idx_notifications_metadata`: JSONB 필드 조회 최적화 (GIN 인덱스)

### 4. 알림 생성 함수 (`create_notification`)
- **타입**: SECURITY DEFINER 함수
- **용도**: RLS를 우회하여 시스템에서 알림 생성
- **파라미터**: user_id, notification_type, title, message, task_id (optional), metadata (optional)
- **반환값**: 생성된 알림의 UUID

### 5. 읽지 않은 알림 수 조회 함수 (`get_unread_notification_count`)
- **타입**: SECURITY DEFINER 함수
- **용도**: 사용자의 읽지 않은 알림 수 조회 (사이드바 배지용)
- **파라미터**: user_id
- **반환값**: 읽지 않은 알림 수 (INTEGER)

### 6. RLS 정책 설정 (4개)
- **SELECT 정책**: 사용자는 자신의 알림만 조회 가능
- **INSERT 정책**: 일반 사용자는 직접 INSERT 불가 (SECURITY DEFINER 함수만 가능)
- **UPDATE 정책**: 사용자는 자신의 알림만 읽음 처리 가능
- **DELETE 정책**: 사용자는 자신의 알림만 삭제 가능

### 7. Realtime 활성화
- **작업**: `supabase_realtime` publication에 notifications 테이블 추가
- **용도**: 프론트엔드에서 실시간 알림 수신 가능

### 8. pg_cron 및 pg_net 확장 활성화
- **pg_cron**: 스케줄된 작업 실행용
- **pg_net**: HTTP 요청 실행용

### 9. 마감일 임박 알림 스케줄러 설정
- **스케줄 이름**: `check_due_date_approaching_daily`
- **실행 시간**: 매일 UTC 00:00 (KST 09:00)
- **Edge Function**: `check-due-date-approaching`
- **용도**: 마감일 2일 전부터 당일까지 알림 생성

### 10. 마감일 초과 알림 스케줄러 설정
- **스케줄 이름**: `check_due_date_exceeded_daily`
- **실행 시간**: 매일 UTC 00:00 (KST 09:00)
- **Edge Function**: `check-due-date-exceeded-notification`
- **용도**: 마감일이 지났고 APPROVED가 아닌 Task에 대해 알림 생성

---

## 배포 전 변경 필요 사항

### ⚠️ 필수 변경 사항

#### 1. 프로젝트 URL 변경
**위치**: 마이그레이션 파일 내 2곳

```sql
-- 현재 (개발 환경)
v_project_url := 'https://mbwmxowoyvaxmtnigjwa.supabase.co';

-- 변경 필요: 실제 프로덕션 프로젝트 URL로 변경
v_project_url := 'https://your-production-project.supabase.co';
```

**변경 위치**:
- 라인 ~150: 마감일 임박 알림 스케줄러 설정 부분
- 라인 ~190: 마감일 초과 알림 스케줄러 설정 부분

#### 2. Service Role Key 설정
**배포 전 또는 배포 후 실행**:

```sql
-- Supabase Dashboard > Settings > API > service_role key 복사 후 실행
ALTER DATABASE postgres SET app.supabase_service_role_key = 'your-service-role-key';
```

**확인 방법**:
```sql
SELECT current_setting('app.supabase_service_role_key', true);
```

---

## Edge Function 배포 확인

마이그레이션 실행 전에 다음 Edge Function들이 배포되어 있어야 합니다:

### 1. check-due-date-approaching (수정된 버전)
- **파일**: `supabase/functions/check-due-date-approaching/index.ts`
- **변경 사항**:
  - 알림 범위: 0-3일 → 0-2일
  - APPROVED 체크: 모든 경우에 적용
  - 알림 메시지: 3일 전 제거

### 2. check-due-date-exceeded-notification (신규)
- **파일**: `supabase/functions/check-due-date-exceeded-notification/index.ts`
- **기능**: 마감일 초과 알림 생성

---

## 마이그레이션 실행 후 확인 사항

### 1. 스케줄러 생성 확인
```sql
SELECT 
  jobid,
  jobname,
  schedule,
  command
FROM cron.job 
WHERE jobname IN ('check_due_date_approaching_daily', 'check_due_date_exceeded_daily');
```

**예상 결과**: 2개의 스케줄이 조회되어야 함

### 2. 함수 생성 확인
```sql
SELECT 
  routine_name,
  routine_type
FROM information_schema.routines
WHERE routine_schema = 'public'
  AND routine_name IN ('create_notification', 'get_unread_notification_count');
```

**예상 결과**: 2개의 함수가 조회되어야 함

### 3. 테이블 및 인덱스 확인
```sql
-- 테이블 확인
SELECT * FROM information_schema.tables 
WHERE table_schema = 'public' AND table_name = 'notifications';

-- 인덱스 확인
SELECT indexname, indexdef 
FROM pg_indexes 
WHERE tablename = 'notifications';
```

**예상 결과**: 
- 테이블 1개
- 인덱스 6개

### 4. RLS 정책 확인
```sql
SELECT 
  schemaname,
  tablename,
  policyname,
  permissive,
  roles,
  cmd,
  qual
FROM pg_policies
WHERE tablename = 'notifications';
```

**예상 결과**: 4개의 정책이 조회되어야 함

### 5. Realtime 활성화 확인
```sql
SELECT 
  pubname,
  tablename
FROM pg_publication_tables
WHERE pubname = 'supabase_realtime'
  AND tablename = 'notifications';
```

**예상 결과**: notifications 테이블이 조회되어야 함

---

## 알림 시스템 동작 요약

### 활성화된 알림 기능

1. **TASK_DUE_DATE_APPROACHING** (마감일 임박)
   - 실행: 매일 09시 (KST)
   - 조건: 마감일 2일 전부터 당일까지
   - 제외: APPROVED 상태인 Task
   - 최대 알림: 3개 (2일 전, 1일 전, 당일)

2. **TASK_DUE_DATE_EXCEEDED** (마감일 초과)
   - 실행: 매일 09시 (KST)
   - 조건: 마감일이 지났고 APPROVED가 아닌 Task
   - 중복 방지: 같은 Task에 대해 한 번만 생성
   - 제외: APPROVED 상태인 Task

### 비활성화된 알림 기능

- **TASK_CREATED**: 제거됨
- **TASK_DELETED**: 제거됨
- **TASK_STATUS_CHANGED**: 제거됨

---

## 주의사항

1. **프로젝트 URL 변경 필수**: 마이그레이션 파일 내 프로젝트 URL을 실제 프로덕션 URL로 변경
2. **Service Role Key 설정 필수**: 스케줄러가 작동하려면 Service Role Key가 설정되어 있어야 함
3. **Edge Function 배포 필수**: 마이그레이션 실행 전에 Edge Function 배포 완료 필요
4. **기존 데이터 보존**: 기존 알림 데이터는 그대로 유지됨
5. **스케줄러 중복 방지**: 기존 스케줄러가 있으면 자동으로 제거 후 재생성

---

## 문제 해결

### 스케줄러가 생성되지 않는 경우
1. Service Role Key가 설정되어 있는지 확인
2. 프로젝트 URL이 올바른지 확인
3. Edge Function이 배포되어 있는지 확인

### 알림이 생성되지 않는 경우
1. Edge Function 로그 확인
2. 스케줄러가 정상 실행되는지 확인
3. Task의 조건 확인 (assignee_id, due_date, task_status)

---

## 작성 일자
2026-01-30

## 마이그레이션 파일 버전
20260130000015_complete_notification_system_final.sql
