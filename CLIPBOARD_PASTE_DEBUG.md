# 클립보드 이미지 붙여넣기 디버깅 가이드

## 현재 구현 상태

클립보드에서 이미지를 붙여넣어 파일로 첨부하는 기능이 구현되어 있습니다.

### 구현 위치
- 파일: `src/pages/task-detail-page.tsx`
- 함수: `handlePaste` (693-800줄)
- 연결: `textarea`의 `onPaste` 이벤트 (1772줄)

## 디버깅 방법

### 1. 브라우저 콘솔 확인
개발자 도구(F12)를 열고 콘솔 탭에서 다음 로그를 확인하세요:

```
[Paste] 클립보드 항목 개수: X
[Paste] 항목 0: { kind: "...", type: "..." }
[Paste] 이미지 발견: image/png
[Paste] 파일 정보: { name: "...", type: "...", size: ... }
[Paste] 생성된 파일: { name: "...", type: "...", size: ... }
[Paste] 이미지 파일 첨부 성공: 1
```

### 2. 가능한 문제점들

#### 문제 1: 클립보드 항목이 감지되지 않음
**증상**: 콘솔에 "클립보드 항목이 없습니다" 메시지
**원인**: 
- 브라우저가 클립보드 API를 지원하지 않음
- HTTPS가 아닌 환경에서 실행 중 (일부 브라우저는 HTTPS 필요)
- 브라우저 권한 문제

**해결책**:
- HTTPS 환경에서 테스트
- 브라우저 콘솔에서 `navigator.clipboard` 확인

#### 문제 2: 이미지가 감지되지 않음
**증상**: 콘솔에 "이미지가 없어 텍스트 붙여넣기 허용" 메시지
**원인**:
- 클립보드에 이미지가 아닌 다른 데이터가 있음
- 캡처 도구가 이미지를 클립보드에 복사하지 않음

**해결책**:
- 캡처 후 이미지 편집기(그림판 등)에서 이미지를 복사하여 테스트
- 다른 이미지 파일을 복사하여 테스트

#### 문제 3: File 생성 실패
**증상**: 콘솔에 "File 생성자 실패" 경고
**원인**:
- 브라우저가 File 생성자를 지원하지 않음 (구형 브라우저)
- 타입스크립트 타입 정의 문제

**해결책**:
- 최신 브라우저 사용 (Chrome, Edge, Firefox 최신 버전)
- Blob 기반 폴백이 자동으로 작동함

#### 문제 4: 이벤트가 발생하지 않음
**증상**: 콘솔에 아무 로그도 없음
**원인**:
- `onPaste` 이벤트가 연결되지 않음
- 다른 요소가 이벤트를 가로챔
- textarea가 비활성화 상태

**해결책**:
- textarea에 포커스가 있는지 확인
- 다른 이벤트 리스너가 `stopPropagation()`을 호출하는지 확인

## 테스트 방법

### Windows
1. `Win + Shift + S`로 스크린샷 캡처
2. 채팅 입력창에 포커스
3. `Ctrl + V`로 붙여넣기

### Mac
1. `Cmd + Shift + 4`로 스크린샷 캡처
2. 채팅 입력창에 포커스
3. `Cmd + V`로 붙여넣기

### 대체 방법
1. 이미지 파일을 그림판/사진 앱에서 열기
2. 이미지 선택 후 `Ctrl/Cmd + C`로 복사
3. 채팅 입력창에 `Ctrl/Cmd + V`로 붙여넣기

## 브라우저 호환성

### 지원되는 브라우저
- Chrome 76+
- Edge 79+
- Firefox 90+
- Safari 13.1+

### File 생성자 지원
- Chrome 50+
- Edge 79+
- Firefox 50+
- Safari 10+

## 추가 개선 가능 사항

### 1. 라이브러리 사용 고려
현재는 네이티브 Clipboard API를 사용하고 있지만, 필요시 다음 라이브러리 고려 가능:

- **clipboard-polyfill**: 구형 브라우저 지원
- **react-dropzone**: 드래그 앤 드롭과 함께 클립보드 지원

하지만 현재 구현으로도 충분히 작동해야 합니다.

### 2. 에러 처리 개선
- 사용자에게 더 명확한 에러 메시지 표시
- 재시도 기능 추가

### 3. 이미지 미리보기
- 붙여넣은 이미지의 미리보기 표시
- 이미지 크기/해상도 확인

## 다음 단계

1. 브라우저 콘솔에서 로그 확인
2. 위의 문제점들 확인
3. 테스트 방법으로 직접 테스트
4. 문제가 지속되면 콘솔 로그를 공유해주세요
