# 이메일 전송 문제 종합 분석 보고서

## 📋 현재 설정 상태 확인

### ✅ Supabase Secrets 설정 (이미지 기준)
이미지에서 확인된 환경 변수:
- ✅ `SUPABASE_URL`: 설정됨 (SHA256: 7e430c359f80bb4303deed74596ca18bbd89ccb7fb3c...)
- ✅ `SUPABASE_ANON_KEY`: 설정됨 (SHA256: 5c4807aea60e601c5873e4f763b0a506618d0c66d804...)
- ✅ `SUPABASE_SERVICE_ROLE_KEY`: 설정됨 (SHA256: 468f967bb1c1b0f980bd40518c371bbba4862ae3556f...)
- ✅ `SUPABASE_DB_URL`: 설정됨 (SHA256: b04ad0f865b7d06354a34a888c14ca10c57b121a580d...)
- ✅ `SMTP_USER`: 설정됨 (SHA256: 433cda823b895918876b47dcebb49dcfc0521390f42a...)
- ✅ `SMTP_PASS`: 설정됨 (SHA256: d07cfb4e65f47115128e52bb646817c3e53fb1a98526...)
- ✅ `FRONTEND_URL`: 설정됨 (SHA256: 8995452fa2bda254a34686a5463cdc28fb125a0c7f07...)

**확인 사항**: 모든 필수 환경 변수가 설정되어 있습니다.

---

## 🔍 백엔드 구조 분석

### 1. 이메일 발송 흐름

```
Task 생성/상태 변경
    ↓
데이터베이스 트리거 실행
    ↓
트리거 함수 (send_task_created_email / send_task_status_change_email)
    ↓
net.http_post로 Edge Function 호출
    ↓
Edge Function (send-task-email)
    ↓
SMTP를 통한 이메일 발송
    ↓
email_logs 테이블에 기록
```

### 2. 트리거 함수 구조

#### 2.1 Task 생성 시 이메일 발송
- **트리거 이름**: `trigger_send_task_created_email`
- **이벤트**: `AFTER INSERT ON public.tasks`
- **함수**: `send_task_created_email()`
- **최신 마이그레이션**: `20250101000026_ensure_hardcoded_email_triggers.sql`

#### 2.2 Task 상태 변경 시 이메일 발송
- **트리거 이름**: `trigger_send_task_status_change_email`
- **이벤트**: `AFTER UPDATE OF task_status ON public.tasks`
- **함수**: `send_task_status_change_email()`
- **최신 마이그레이션**: `20250101000026_ensure_hardcoded_email_triggers.sql`

### 3. 트리거 함수 설정 (하드코딩 방식)

**현재 설정된 값**:
- **Edge Function URL**: `https://dcovjxmrqomuuwcgiwie.supabase.co/functions/v1/send-task-email`
- **Service Role Key**: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRjb3ZqeG1ycW9tdXV3Y2dpd2llIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NjAwNjMyNywiZXhwIjoyMDgxNTgyMzI3fQ.0nK3qmclkR2urRsAytgRthpdb-OwaX6rJLLiOIsQH1o`

**확인**: 사용자가 제공한 Service Role Key와 마이그레이션 파일의 키가 **동일**합니다.

### 4. net.http_post 함수 시그니처

**최신 마이그레이션에서 사용하는 형식**:
```sql
PERFORM net.http_post(
  url := function_url,
  body := request_body,
  params := '{}'::jsonb,
  headers := jsonb_build_object(
    'Content-Type', 'application/json',
    'Authorization', 'Bearer [SERVICE_ROLE_KEY]'
  )
);
```

**주의**: 이 형식이 Supabase의 `pg_net` 확장에서 실제로 지원되는지 확인 필요합니다.

---

## ⚠️ 발견된 잠재적 문제점

### 문제 1: net.http_post 함수 시그니처 불일치

**발견된 두 가지 형식**:

#### 형식 A (구버전):
```sql
PERFORM net.http_post(
  url := function_url,
  headers := jsonb_build_object(...),
  body := request_body::text
);
```

#### 형식 B (신버전 - 현재 사용 중):
```sql
PERFORM net.http_post(
  url := function_url,
  body := request_body,
  params := '{}'::jsonb,
  headers := jsonb_build_object(...)
);
```

**문제**: Supabase의 `pg_net` 확장에서 실제로 지원하는 형식이 무엇인지 확인이 필요합니다.

**해결 방법**: Supabase 문서를 확인하거나, 실제 데이터베이스에서 함수 시그니처를 확인해야 합니다.

### 문제 2: 트리거 실행 여부 확인 불가

**현재 상태**:
- 트리거 함수 내부에 로깅이 없음
- `RAISE WARNING`만 사용하지만 실제로 로그에 남는지 불확실
- 트리거가 실행되는지 확인할 방법이 없음

**해결 방법**: 트리거 함수에 로깅 추가 필요

### 문제 3: Edge Function 호출 실패 시 조용히 실패

**현재 상태**:
```sql
EXCEPTION
  WHEN OTHERS THEN
    RAISE WARNING 'Failed to send email notification: %', SQLERRM;
    RETURN NEW;
END;
```

- 에러가 발생해도 트랜잭션은 성공
- 실제 에러 내용을 확인하기 어려움
- Edge Function이 호출되지 않아도 알 수 없음

### 문제 4: Edge Function 배포 상태 불확실

**확인 필요 사항**:
- Edge Function이 실제로 배포되어 있는지
- Edge Function URL이 올바른지
- Edge Function이 호출되는지

---

## 🔧 진단 및 해결 방안

### 1단계: 데이터베이스 상태 확인

**실행할 SQL 쿼리** (`EMAIL_DIAGNOSIS.sql` 파일 참조):

```sql
-- 1. 트리거 존재 및 활성화 확인
SELECT 
  tgname AS trigger_name,
  CASE 
    WHEN tgenabled = 'O' THEN '활성화됨 ✓'
    WHEN tgenabled = 'D' THEN '비활성화됨 ✗'
    ELSE '알 수 없음'
  END AS status,
  tgrelid::regclass AS table_name
FROM pg_trigger
WHERE tgname IN ('trigger_send_task_created_email', 'trigger_send_task_status_change_email');

-- 2. 트리거 함수 존재 및 설정 확인
SELECT 
  proname AS function_name,
  CASE 
    WHEN prosrc LIKE '%dcovjxmrqomuuwcgiwie.supabase.co%' THEN '하드코딩됨 ✓'
    ELSE '확인 필요'
  END AS url_config,
  CASE 
    WHEN prosrc LIKE '%Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9%' THEN '하드코딩됨 ✓'
    ELSE '확인 필요'
  END AS auth_config
FROM pg_proc
WHERE proname IN ('send_task_created_email', 'send_task_status_change_email');

-- 3. pg_net 확장 확인
SELECT 
  extname AS extension_name,
  extversion AS version
FROM pg_extension
WHERE extname = 'pg_net';

-- 4. net.http_post 함수 시그니처 확인
SELECT 
  p.proname AS function_name,
  pg_get_function_arguments(p.oid) AS arguments,
  pg_get_function_result(p.oid) AS return_type
FROM pg_proc p
JOIN pg_namespace n ON p.pronamespace = n.oid
WHERE n.nspname = 'net' 
  AND p.proname = 'http_post';

-- 5. 최근 이메일 로그 확인
SELECT 
  id,
  task_id,
  recipient_email,
  status,
  error_message,
  created_at
FROM public.email_logs
ORDER BY created_at DESC
LIMIT 20;
```

### 2단계: Edge Function 배포 확인

**터미널에서 실행**:
```bash
# Edge Function 목록 확인
supabase functions list --project-ref dcovjxmrqomuuwcgiwie

# Edge Function 로그 확인
supabase functions logs send-task-email --project-ref dcovjxmrqomuuwcgiwie
```

### 3단계: Edge Function 직접 테스트

**curl 명령어로 직접 호출**:
```bash
curl -X POST \
  'https://dcovjxmrqomuuwcgiwie.supabase.co/functions/v1/send-task-email' \
  -H 'Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRjb3ZqeG1ycW9tdXV3Y2dpd2llIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NjAwNjMyNywiZXhwIjoyMDgxNTgyMzI3fQ.0nK3qmclkR2urRsAytgRthpdb-OwaX6rJLLiOIsQH1o' \
  -H 'Content-Type: application/json' \
  -d '{
    "eventType": "STATUS_CHANGED",
    "taskId": "test-task-id",
    "oldStatus": "ASSIGNED",
    "newStatus": "IN_PROGRESS",
    "assignerEmail": "your-email@example.com",
    "assigneeEmail": "your-email@example.com",
    "assignerName": "지시자",
    "assigneeName": "담당자",
    "taskTitle": "테스트 Task",
    "projectTitle": "테스트 프로젝트",
    "recipients": ["assigner", "assignee"]
  }'
```

**예상 결과**:
- 성공 시: `{"success": true, "message": "Emails sent successfully", ...}`
- 실패 시: 에러 메시지 확인

### 4단계: 트리거 함수에 로깅 추가

**개선된 트리거 함수** (로깅 추가):
```sql
CREATE OR REPLACE FUNCTION public.send_task_created_email()
RETURNS TRIGGER AS $$
DECLARE
  -- ... 기존 변수들 ...
  http_response_id BIGINT;
BEGIN
  -- 로깅: 트리거 실행 확인
  RAISE NOTICE '[EMAIL_TRIGGER] Task created: %', NEW.id;
  
  -- ... 기존 코드 ...
  
  -- Edge Function 호출
  SELECT net.http_post(
    url := function_url,
    body := request_body,
    params := '{}'::jsonb,
    headers := jsonb_build_object(
      'Content-Type', 'application/json',
      'Authorization', 'Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImRjb3ZqeG1ycW9tdXV3Y2dpd2llIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2NjAwNjMyNywiZXhwIjoyMDgxNTgyMzI3fQ.0nK3qmclkR2urRsAytgRthpdb-OwaX6rJLLiOIsQH1o'
    )
  ) INTO http_response_id;
  
  -- 로깅: HTTP 요청 ID 확인
  RAISE NOTICE '[EMAIL_TRIGGER] HTTP request sent: %', http_response_id;
  
  RETURN NEW;
EXCEPTION
  WHEN OTHERS THEN
    RAISE WARNING '[EMAIL_TRIGGER] Failed to send email: %', SQLERRM;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

---

## 📝 체크리스트

다음 항목들을 순서대로 확인하세요:

- [ ] **1. 데이터베이스 트리거 확인**
  - [ ] 트리거가 존재하고 활성화되어 있는지
  - [ ] 트리거 함수가 하드코딩 방식인지
  - [ ] `pg_net` 확장이 설치되어 있는지

- [ ] **2. Edge Function 배포 확인**
  - [ ] Edge Function이 배포되어 있는지
  - [ ] Edge Function URL이 올바른지
  - [ ] Edge Function 로그에서 호출 확인

- [ ] **3. 환경 변수 확인**
  - [ ] `SMTP_USER`가 올바르게 설정되어 있는지
  - [ ] `SMTP_PASS`가 올바르게 설정되어 있는지
  - [ ] `SUPABASE_URL`이 올바르게 설정되어 있는지
  - [ ] `SUPABASE_SERVICE_ROLE_KEY`가 올바르게 설정되어 있는지
  - [ ] `FRONTEND_URL`이 올바르게 설정되어 있는지

- [ ] **4. 이메일 로그 확인**
  - [ ] `email_logs` 테이블에 발송 시도 기록이 있는지
  - [ ] 실패한 이메일이 있는지
  - [ ] 에러 메시지 확인

- [ ] **5. 수동 테스트**
  - [ ] Edge Function 직접 호출 테스트
  - [ ] Task 상태 변경 후 로그 확인
  - [ ] 실제 이메일 수신 확인

---

## 🚀 권장 조치 사항

### 즉시 실행할 항목:

1. **데이터베이스 상태 확인**
   - `EMAIL_DIAGNOSIS.sql` 파일을 Supabase Dashboard SQL Editor에서 실행
   - 트리거 및 함수 상태 확인

2. **Edge Function 로그 확인**
   - Supabase Dashboard → Edge Functions → send-task-email → Logs
   - 최근 호출 기록 확인

3. **이메일 로그 확인**
   - `email_logs` 테이블에서 최근 발송 시도 확인
   - 실패한 이메일의 에러 메시지 확인

4. **Edge Function 직접 테스트**
   - curl 명령어로 Edge Function 직접 호출
   - SMTP 설정이 올바른지 확인

### 문제 지속 시:

1. **트리거 함수에 로깅 추가**
   - 트리거 실행 여부 확인
   - HTTP 요청 ID 확인

2. **net.http_post 시그니처 확인**
   - Supabase 문서 확인
   - 실제 데이터베이스에서 함수 시그니처 확인

3. **대안 방법 고려**
   - Edge Function 대신 다른 방식 사용
   - 큐 시스템 도입 고려

---

## 📚 참고 파일

- `EMAIL_DIAGNOSIS.sql`: 데이터베이스 상태 확인 쿼리
- `EMAIL_BACKEND_ANALYSIS.md`: 백엔드 구조 분석
- `EMAIL_DEBUG_GUIDE.md`: 디버깅 가이드
- `EMAIL_TROUBLESHOOTING_GUIDE.md`: 문제 해결 가이드
- `supabase/functions/send-task-email/index.ts`: Edge Function 코드
- `supabase/migrations/20250101000026_ensure_hardcoded_email_triggers.sql`: 최신 트리거 함수

---

## 🔑 핵심 확인 사항 요약

1. **Service Role Key**: ✅ 사용자 제공 키와 마이그레이션 파일의 키가 동일함
2. **환경 변수**: ✅ 이미지에서 모든 필수 환경 변수가 설정되어 있음
3. **트리거 함수**: ✅ 하드코딩 방식으로 설정되어 있음
4. **Edge Function**: ⚠️ 배포 상태 및 호출 여부 확인 필요
5. **net.http_post**: ⚠️ 함수 시그니처 확인 필요

**다음 단계**: 위의 체크리스트를 순서대로 확인하여 정확한 원인을 파악하세요.


