# 알림 시스템 기능 정리

## 개요
Tasko 웹 애플리케이션의 알림 시스템은 Task 마감일 관련 알림을 자동으로 생성하여 담당자에게 전송합니다.

---

## 활성화된 알림 기능

### 1. TASK_DUE_DATE_APPROACHING (마감일 임박 알림)

#### 실행 방식
- **실행 주기**: 매일 UTC 00:00 (KST 09:00) 자동 실행
- **Edge Function**: `check-due-date-approaching`
- **스케줄러**: `pg_cron`을 통한 자동 실행

#### 알림 생성 조건
- **대상**: `assignee_id`가 있고 `due_date`가 있는 Task
- **범위**: 마감일이 **0-3일 남은** Task (현재 설정)
- **중복 방지**: 같은 Task, 같은 `days_remaining` 값의 알림이 이미 있으면 생성하지 않음

#### 알림 내용
- **3일 전**: "Task 마감일이 3일 남았습니다"
- **2일 전**: "Task 마감일이 2일 남았습니다"
- **1일 전**: "Task 마감일이 1일 남았습니다"
- **당일**: "Task 마감일이 오늘입니다" (APPROVED 상태가 아닌 경우만)

#### 현재 문제점
1. **3일 전 알림 포함**: 마감일 2일 전부터 알림이 와야 하는데 현재는 3일 전부터 시작됨
2. **APPROVED 체크 부족**: 당일 알림만 APPROVED 상태를 체크하고, 2일 전/1일 전 알림은 APPROVED 상태여도 생성됨

#### 수정 필요 사항
- **범위 변경**: 0-3일 → **0-2일** (2일 전부터 시작)
- **APPROVED 체크 강화**: 모든 경우에 APPROVED 상태면 알림 생성하지 않음

---

## 제거된 알림 기능

### 1. TASK_CREATED (작업 생성 알림)
- **상태**: 제거됨
- **이유**: 알림이 너무 많아짐
- **제거 일자**: 2026-01-30
- **마이그레이션**: `20260130000013_remove_task_created_deleted_notifications.sql`

### 2. TASK_DELETED (작업 삭제 알림)
- **상태**: 제거됨
- **이유**: 구현 미숙으로 인한 문제
- **제거 일자**: 2026-01-30
- **마이그레이션**: `20260130000013_remove_task_created_deleted_notifications.sql`

### 3. TASK_STATUS_CHANGED (상태 변경 알림)
- **상태**: 제거됨
- **이유**: 알림이 너무 많아짐
- **제거 일자**: 2026-01-30
- **마이그레이션**: `20260130000008_remove_status_changed_and_fix_deleted_notification.sql`

---

## 미구현 알림 기능

### 1. TASK_DUE_DATE_EXCEEDED (마감일 초과 알림)
- **상태**: 미구현
- **현재 상태**: 데이터베이스 enum에만 존재, 실제 알림 생성 로직 없음
- **Edge Function**: `check-due-date-exceeded` 존재하지만 알림 생성하지 않음
- **용도**: Task 생성 시 일정이 마감일을 초과했는지 확인만 함 (경고 토스트만 표시)

---

## 알림 시스템 구조

### 데이터베이스
- **테이블**: `notifications`
- **타입**: `notification_type` enum
- **함수**: `create_notification()` - 알림 생성 함수
- **함수**: `get_unread_notification_count()` - 읽지 않은 알림 수 조회

### Edge Function
- **check-due-date-approaching**: 마감일 임박 알림 생성 (매일 자동 실행)

### 스케줄러
- **pg_cron**: 매일 UTC 00:00 (KST 09:00)에 Edge Function 자동 호출

### 프론트엔드
- **알림 페이지**: `src/pages/notifications-page.tsx`
- **사이드바 배지**: `src/components/layout/app-sidebar.tsx`
- **Realtime 구독**: `src/hooks/queries/use-realtime-notifications.ts`

---

## 수정 계획

### 1. 알림 범위 변경 (0-3일 → 0-2일)
**파일**: `supabase/functions/check-due-date-approaching/index.ts`

**변경 사항**:
```typescript
// 현재
if (daysRemaining < 0 || daysRemaining > 3) {
  continue;
}

// 수정 후
if (daysRemaining < 0 || daysRemaining > 2) {
  continue;
}
```

**효과**: 마감일 2일 전부터 알림 시작 (2일 전, 1일 전, 당일 = 총 3개)

### 2. APPROVED 상태 체크 강화
**파일**: `supabase/functions/check-due-date-approaching/index.ts`

**변경 사항**:
```typescript
// 현재: 당일만 APPROVED 체크
if (daysRemaining === 0 && task.task_status === "APPROVED") {
  continue;
}

// 수정 후: 모든 경우에 APPROVED 체크
if (task.task_status === "APPROVED") {
  console.log(
    `[check-due-date-approaching] Task ${task.id}: APPROVED 상태이므로 알림 생성하지 않음`
  );
  continue;
}
```

**효과**: Task가 APPROVED 상태가 되면 2일 전, 1일 전, 당일 모두 알림 생성하지 않음

### 3. 알림 메시지 수정
**파일**: `supabase/functions/check-due-date-approaching/index.ts`

**변경 사항**:
```typescript
// 현재: 3일 전 라벨 포함
const daysRemainingLabel =
  daysRemaining === 0
    ? "당일"
    : daysRemaining === 1
    ? "1일 전"
    : daysRemaining === 2
    ? "2일 전"
    : "3일 전"; // 제거

// 수정 후: 2일 전까지만
const daysRemainingLabel =
  daysRemaining === 0
    ? "당일"
    : daysRemaining === 1
    ? "1일 전"
    : "2일 전";
```

---

## 알림 생성 시나리오 (수정 후)

### 예시: Task 마감일이 1월 22일인 경우

#### 1월 20일 09시 (마감일 2일 전)
- `daysRemaining = 2`
- 조건: 0-2일 범위 내
- APPROVED 체크: `task_status !== "APPROVED"` → 알림 생성
- 결과: "Task 마감일이 2일 남았습니다" 알림 전송

#### 1월 21일 09시 (마감일 1일 전)
- `daysRemaining = 1`
- 조건: 0-2일 범위 내
- APPROVED 체크: `task_status !== "APPROVED"` → 알림 생성
- 결과: "Task 마감일이 1일 남았습니다" 알림 전송

#### 1월 22일 09시 (마감일 당일)
- `daysRemaining = 0`
- 조건: 0-2일 범위 내
- APPROVED 체크: `task_status !== "APPROVED"` → 알림 생성
- 결과: "Task 마감일이 오늘입니다" 알림 전송

### APPROVED 상태인 경우
- **2일 전**: APPROVED 상태 → 알림 생성 안 함
- **1일 전**: APPROVED 상태 → 알림 생성 안 함
- **당일**: APPROVED 상태 → 알림 생성 안 함

**총 알림 개수**: 최대 3개 (2일 전, 1일 전, 당일) - APPROVED 상태가 아닌 경우만

---

## 관련 파일

### 마이그레이션 파일
- `supabase/migrations/notification/20260130000001_create_notifications_system.sql`
- `supabase/migrations/notification/20260130000002_create_notification_functions.sql`
- `supabase/migrations/notification/20260130000007_create_due_date_approaching_scheduler.sql`
- `supabase/migrations/notification/20260130000013_remove_task_created_deleted_notifications.sql`

### Edge Function
- `supabase/functions/check-due-date-approaching/index.ts`

### 프론트엔드
- `src/api/notification.ts`
- `src/pages/notifications-page.tsx`
- `src/hooks/queries/use-notifications.ts`
- `src/hooks/queries/use-realtime-notifications.ts`
- `src/hooks/mutations/use-notification.ts`
- `src/components/layout/app-sidebar.tsx`

---

## 참고 사항

1. **중복 방지**: 같은 Task, 같은 `days_remaining` 값의 알림이 이미 있으면 생성하지 않음
2. **Realtime 업데이트**: 알림 생성 시 프론트엔드에서 자동으로 업데이트됨
3. **읽지 않은 알림 수**: 사이드바에 배지로 표시됨
4. **알림 삭제**: 사용자가 수동으로 삭제 가능
5. **알림 읽음 처리**: 개별 또는 전체 읽음 처리 가능

---

## 작성 일자
2026-01-30

## 최종 수정 일자
2026-01-30
